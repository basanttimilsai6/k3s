apiVersion: v1
kind: Service
metadata:
  name: fastpro-service
spec:
  selector:
    app: fastpro
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
  type: NodePort